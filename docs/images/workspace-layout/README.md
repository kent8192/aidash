# Workspace layout review captures

These browser captures use sample data from `web/tests/collaboration-fixture.ts`,
Japanese text, and device scale factor 1. They contain no production account data.

- `before.png`: base `10fb917`, 1280 × 800, light channel view.
- `workspace-light.png`: updated workspace, 1280 × 800, same sample channel.
- `workspace-thread-tall.png`: updated workspace, 1280 × 960, dark thread view.

The updated captures are generated by the reference-layout test in
`web/tests/collaboration.spec.ts`. See `design-qa.md` at the project root for the
comparison record and validation limits.
